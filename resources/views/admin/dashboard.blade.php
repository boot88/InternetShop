@extends('layouts.app')
@section('title','Управление магазином')
@section('robots','noindex,nofollow')
@section('content')
<section class="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8">@include('admin.partials.nav')<h1 class="text-3xl font-semibold text-slate-950">Управление магазином</h1><div class="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">@foreach([['Все товары',$productsCount],['Активные товары',$activeProductsCount],['Заказы в работе',$pendingOrdersCount],['Отзывы на проверке',$pendingReviewsCount]] as [$label,$value])<div class="rounded-2xl bg-white p-5 ring-1 ring-slate-200"><p class="text-sm text-slate-500">{{ $label }}</p><p class="mt-2 text-3xl font-semibold text-slate-950">{{ $value }}</p></div>@endforeach</div><div class="mt-8 rounded-3xl bg-white p-6 ring-1 ring-slate-200"><div class="flex justify-between"><h2 class="text-lg font-semibold">Последние заказы</h2><a href="{{ route('admin.orders.index') }}" class="text-sm font-semibold text-indigo-700">Все заказы</a></div><div class="mt-4 divide-y divide-slate-100">@forelse($recentOrders as $order)<a href="{{ route('admin.orders.show',$order) }}" class="grid gap-2 py-3 hover:text-indigo-700 sm:grid-cols-[1fr_auto_auto]"><span class="font-medium">{{ $order->order_number }}</span><span class="text-sm">{{ $order->status_label }}</span><span class="font-semibold">{{ number_format($order->total,0,',',' ') }} ₽</span></a>@empty<p class="py-5 text-sm text-slate-500">Заказов пока нет.</p>@endforelse</div></div></section>
@endsection

@extends('layouts.app')
@section('title','Отзывы — управление')
@section('robots','noindex,nofollow')
@section('content')
<section class="mx-auto max-w-6xl px-4 py-8 sm:px-6 lg:px-8">@include('admin.partials.nav')<h1 class="text-3xl font-semibold text-slate-950">Отзывы</h1><div class="mt-6 space-y-4">@forelse($reviews as $review)<article class="rounded-2xl bg-white p-5 ring-1 ring-slate-200"><div class="flex flex-wrap justify-between gap-3"><div><p class="font-semibold">{{ $review->product?->name }}</p><p class="text-sm text-slate-500">{{ $review->user?->name }} · {{ $review->rating }}/5 · {{ $review->created_at->format('d.m.Y') }}</p></div><span class="text-sm font-semibold {{ $review->is_approved?'text-emerald-700':'text-amber-700' }}">{{ $review->is_approved?'Опубликован':'На проверке' }}</span></div>@if($review->title)<h2 class="mt-3 font-semibold">{{ $review->title }}</h2>@endif<p class="mt-2 text-sm leading-6 text-slate-600">{{ $review->comment }}</p><div class="mt-4 flex gap-3"><form action="{{ route('admin.reviews.update',$review) }}" method="POST">@csrf @method('PATCH')<input type="hidden" name="is_approved" value="{{ $review->is_approved?0:1 }}"><button class="rounded-xl bg-slate-900 px-4 py-2 text-sm font-semibold text-white">{{ $review->is_approved?'Скрыть':'Опубликовать' }}</button></form><form action="{{ route('admin.reviews.destroy',$review) }}" method="POST" onsubmit="return confirm('Удалить отзыв?')">@csrf @method('DELETE')<button class="rounded-xl border border-rose-200 px-4 py-2 text-sm font-semibold text-rose-700">Удалить</button></form></div></article>@empty<div class="rounded-2xl bg-white p-8 text-center text-slate-500 ring-1 ring-slate-200">Отзывов пока нет.</div>@endforelse</div><div class="mt-6">{{ $reviews->links() }}</div></section>
@endsection

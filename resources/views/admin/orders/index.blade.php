@extends('layouts.app')
@section('title','Заказы — управление')
@section('robots','noindex,nofollow')
@section('content')
<section class="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8">@include('admin.partials.nav')<div class="flex flex-wrap items-end justify-between gap-4"><h1 class="text-3xl font-semibold text-slate-950">Заказы</h1><form><select name="status" class="rounded-xl border-slate-300"><option value="">Все статусы</option>@foreach(['pending'=>'Ожидают','processing'=>'В обработке','shipped'=>'Доставка','delivered'=>'Доставлены','cancelled'=>'Отменены','refunded'=>'Возврат'] as $value=>$label)<option value="{{ $value }}" @selected($status===$value)>{{ $label }}</option>@endforeach</select><button class="ml-2 rounded-xl bg-slate-900 px-4 py-2 text-sm font-semibold text-white">Применить</button></form></div><div class="mt-6 overflow-x-auto rounded-2xl bg-white ring-1 ring-slate-200"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-slate-500"><tr><th class="px-4 py-3">Заказ</th><th class="px-4 py-3">Покупатель</th><th class="px-4 py-3">Статус</th><th class="px-4 py-3">Сумма</th><th></th></tr></thead><tbody class="divide-y divide-slate-100">@foreach($orders as $order)<tr><td class="px-4 py-3"><p class="font-semibold">{{ $order->order_number }}</p><p class="text-xs text-slate-500">{{ $order->created_at->format('d.m.Y H:i') }} · {{ $order->items_count }} поз.</p></td><td class="px-4 py-3">{{ $order->user?->name ?: 'Гостевой заказ' }}</td><td class="px-4 py-3">{{ $order->status_label }}</td><td class="px-4 py-3 font-semibold">{{ number_format($order->total,0,',',' ') }} ₽</td><td class="px-4 py-3 text-right"><a href="{{ route('admin.orders.show',$order) }}" class="font-semibold text-indigo-700">Открыть</a></td></tr>@endforeach</tbody></table></div><div class="mt-6">{{ $orders->links() }}</div></section>
@endsection

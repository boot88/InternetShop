@extends('layouts.app')
@section('title','Товары — управление')
@section('robots','noindex,nofollow')
@section('content')
<section class="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8">@include('admin.partials.nav')<div class="flex flex-wrap items-end justify-between gap-4"><div><h1 class="text-3xl font-semibold text-slate-950">Товары</h1><p class="mt-2 text-sm text-slate-500">Цена, остаток, описание и фотографии.</p></div><div class="flex flex-wrap gap-2"><a href="{{ route('admin.products.create') }}" class="rounded-xl bg-indigo-600 px-4 py-2 text-sm font-semibold text-white">Добавить товар</a><form><input name="search" value="{{ $search }}" placeholder="Название или артикул" class="rounded-xl border-slate-300"><button class="ml-2 rounded-xl bg-slate-900 px-4 py-2 text-sm font-semibold text-white">Найти</button></form></div></div><div class="mt-6 overflow-x-auto rounded-2xl bg-white ring-1 ring-slate-200"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-slate-500"><tr><th class="px-4 py-3">Товар</th><th class="px-4 py-3">Цена</th><th class="px-4 py-3">Остаток</th><th class="px-4 py-3">Фото</th><th class="px-4 py-3">Статус</th><th></th></tr></thead><tbody class="divide-y divide-slate-100">@foreach($products as $product)<tr><td class="px-4 py-3"><p class="font-semibold text-slate-900">{{ $product->name }}</p><p class="text-xs text-slate-500">{{ $product->sku }} · {{ $product->brand?->name }}</p></td><td class="px-4 py-3 whitespace-nowrap">{{ number_format($product->price,0,',',' ') }} ₽</td><td class="px-4 py-3">{{ $product->stock?->quantity ?? 0 }}</td><td class="px-4 py-3">{{ $product->images_count }}</td><td class="px-4 py-3"><span class="{{ $product->is_active?'text-emerald-700':'text-slate-500' }}">{{ $product->is_active?'Активен':'Скрыт' }}</span></td><td class="px-4 py-3 text-right"><a href="{{ route('admin.products.edit',$product) }}" class="font-semibold text-indigo-700">Изменить</a></td></tr>@endforeach</tbody></table></div><div class="mt-6">{{ $products->links() }}</div></section>
@endsection
